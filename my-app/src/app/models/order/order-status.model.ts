// models/order/order-status.model.ts
export interface OrderStatus {
  id_statut: number;  // Changé de statusId pour correspondre au backend
  label: string;    // Changé de label pour correspondre au backend
}
